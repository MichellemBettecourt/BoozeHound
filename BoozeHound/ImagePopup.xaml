<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BoozeHound.ImagePopup"
             Style="{StaticResource popContent}">
    <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
        <StackLayout BackgroundColor="White" Orientation="Vertical" Spacing="0" Padding="0">
            <Image x:Name="LargeImage" Aspect="AspectFill"/>
            <Button x:Name="CloseButton" Text="Close" Clicked="CloseButton_Clicked" BorderWidth="0" />
        </StackLayout>
    </StackLayout>
</ContentView>